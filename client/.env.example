# Base URL of the backend API.
# In development the Vite dev server proxies /api to the backend,
# so you can leave this as /api. For a separately-hosted API, set the full URL,
# e.g. VITE_API_URL=https://api.your-domain.com/api
VITE_API_URL=/api
